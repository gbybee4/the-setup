---
- name: Prepare
  hosts: all
  tasks:
    - name: Install prerequisites
      ansible.builtin.apt:
        name:
          - sudo
          - gpg
          - software-properties-common
          - fontconfig
        state: present
        update_cache: true
